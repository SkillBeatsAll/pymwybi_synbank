# Stage 1 (cleaning) and stage 2 (feature layer) both run on DuckDB alone.
duckdb>=1.4.4,<2.0

# pandas is used for readable tabular output in analysis/, the wallet model (stage 3,
# which runs in pandas rather than SQL), and the Streamlit dashboard (stage 4).
pandas>=2.2,<3.0

pytest>=8.0,<9.0
numpy>=1.26,<3.0
scipy>=1.12,<2.0
streamlit>=1.38,<2.0
plotly>=5.22,<6.0
anthropic>=0.34,<1.0
jupyter>=1.0,<2.0

# Stage 5 (the copilot) only. OPTIONAL: the copilot detects its absence and
# answers deterministically instead, and the whole test suite passes without it.
# Reaches DeepSeek or NVIDIA NIM through their OpenAI-compatible endpoints.
openai>=1.40

# Stage 6 (the dashboard). The API layer and its test client. No build step and
# no front-end dependency: the browser gets vanilla JS and inline SVG.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27

# The submission notebook (submission/SynBank_Share_of_Wallet_Analysis.ipynb) only.
# The notebook imports the production modules and reads the published Parquet; these
# are for running it and drawing its charts. No pipeline module depends on them, and
# the full test suite passes without them installed.
matplotlib>=3.9
ipykernel>=6.29
nbformat>=5.10
nbconvert>=7.16
jupyterlab>=4.2
